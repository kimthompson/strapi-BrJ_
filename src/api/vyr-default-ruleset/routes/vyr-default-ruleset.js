'use strict';

/**
 * vyr-default-ruleset router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::vyr-default-ruleset.vyr-default-ruleset');
