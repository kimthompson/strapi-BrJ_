'use strict';

/**
 * vyr-default-ruleset controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::vyr-default-ruleset.vyr-default-ruleset');
